#from distutils.core import setup
from setuptools import setup

setup(name='AnkiVocParser',
      version='0.1',
      description='OCR and Parser for vocabulary extraction from tables into Anki format',
      author='terminator-ger',
      packages=['anki_voc_parser'],
      )
